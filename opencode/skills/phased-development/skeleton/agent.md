# `AGENTS.md`

Append this pointer without replacing unrelated repository instructions:

```markdown
Load the `phased-development` skill for project work.
Before editing, read `.project/project.json` and `.project/STATE.md`.
```
